<h1>Success!</h1>

<ul>
  <% @user.images.each do |image| %>
  <li><%= image_tag(image.photo) %><%= image.description %>
  <%= link_to "Edit", edit_user_image_path(@user, image) %>
  </li>
  <% end %>
</ul>

<%= link_to "Add an image", new_user_image_path(@user) %>
