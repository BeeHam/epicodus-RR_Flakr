<h2>Welcome to Flakr, a site about </h2>

<ul>
  <% @users.each do |user| %>
  <li><%= link_to user.username, user_path(user) %></li>
  <% end %>
</ul>

<hr>

<ul>
  <% @images.each do |image| %>
  <li><%= link_to image_tag(image.photo), user_path(image.user)  %></li>
  <% end %>
</ul>
